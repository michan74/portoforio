## 開発環境

- 起動
```
docker compose build
docker compose up -d
```

### バックエンドSand BOX環境
- 起動
```
docker compose run --rm app npx ampx sandbox
```
- 削除
```
docker compose run --rm app npx ampx sandbox delete
```
